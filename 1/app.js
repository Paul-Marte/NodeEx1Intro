const names = ['Sam', 'Bruno', 'Lisa'];

for (const name of names) {
    console.log(name);
}

console.log(process);

console.log(process.pid);

console.log(process.version);

console.log(process.platform);